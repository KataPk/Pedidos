{

    // Tentar depois

    // $(document).ready(function () {
    //     function atualizarMesas() {
    //         console.log("Atualizando...")
    //         const mesasContainer = document.getElementById('mesas-container');
    //         if (mesasContainer) {
    //             $.ajax({
    //                 type: 'GET',
    //                 url: '/api/user/mesas/atualizar',
    //                 success: function (data) {
    //                     // Verifique novamente se o elemento ainda existe antes de atualizá-lo
    //                     const updatedContainer = document.getElementById('mesas-container');
    //                     if (updatedContainer) {
    //                         updatedContainer.innerHTML = '';
    //                         data.mesas.forEach(mesa => {
    //                             const mesaElement = document.createElement('div');
    //                             mesaElement.textContent = `Mesa ${mesa.numMesa}`;
    //                             updatedContainer.appendChild(mesaElement);
    //                         });
    //                         atualizarModaisMesas(data);
    //                     }
    //                 },
    //                 error: function (error) {
    //                     console.log('Erro ao atualizar as mesas:', error.responseText);
    //                 }
    //             });
    //         }
    //     }
    //
    //     // Chame a função de atualização de mesas a cada 1 minuto (60.000 milissegundos)
    //     setInterval(atualizarMesas, 20000);
    //
    //     // Função para atualizar o conteúdo dos modais de mesas com os dados atualizados
    //     function atualizarModaisMesas(mesas) {
    //         mesas.forEach(function (mesa) {
    //             var modal = $('#ModalMesa' + mesa.numMesa);
    //             if (modal.length) {
    //                 modal.find('.modal-title').text('Mesa ' + mesa.numMesa);
    //                 modal.find('.clientNameMesa').val('');
    //                 modal.find('.mesaId').val(mesa.id);
    //             }
    //         });
    //     }
    // });


    // ADM
}
{

}














